{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "entra_service_principals",
  "version": "1.0.0",
  "type": "object",
  "description": "Service principal objects from Microsoft Graph (Get-MgServicePrincipal).",
  "primaryKey": ["Id"],
  "properties": {
    "Id": { "type": "string" },
    "AppId": { "type": "string" },
    "DisplayName": { "type": "string" },
    "ServicePrincipalNames": { "type": ["string", "null"], "description": "Serialized JSON array when exported to CSV" }
  },
  "required": [ "Id", "AppId", "DisplayName" ]
}
